COMPILER = gmcs
COMPILER_FLAGS = -r:Tao.OpenGl -r:Tao.Sdl -r:main.dll
EXECUTABLE = viewer.exe

SOURCES = MonoMain.cs

$(EXECUTABLE):
	cp ../Main/main.dll .
	$(COMPILER) $(COMPILER_FLAGS) $(SOURCES) -out:$(EXECUTABLE)
	
clean:
	rm $(EXECUTABLE)